# LightNet CLI

Available commands:

- `npx @lightnet/cli translation-status`
- `npx @lightnet/cli migrate-to-v4 --locales en,de,ar`
- `npx @lightnet/cli add-site-locale --locale fr --source-locale en`

Run commands from your LightNet project root.
