---
"lightnet": major
---

Rename internal translation keys from `ln.*` to `ln_` + camelCase (for example `ln.header.open-main-menu` → `ln_headerOpenMainMenu`) and camelize all user translation keys (for example `home.bbq.sign-up-now` → `homeBbqSignUpNow`). This is a breaking change with no compatibility alias.
