---
"@lightnet/sveltia-admin": minor
---

Add optional `logo` support to `lightnetSveltiaAdmin({ ... })`.

If `sveltiaAdmin.logo` is set, the admin UI uses it.
If it is not set, the admin UI now falls back to the LightNet `logo` config.
