---
"@lightnet/sveltia-admin": patch
---

Move `lightnet` from a direct dependency to a peer dependency for `@lightnet/sveltia-admin`.

Consumer sites should install `lightnet` directly alongside `@lightnet/sveltia-admin`.
